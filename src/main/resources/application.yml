spring:
  application:
    name: service_chat-service

  datasource:
<<<<<<< HEAD
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3307}/${DB_NAME:passit_db}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul
=======
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3307}/${DB_NAME:passit_db}
           ?useUnicode=true
           &characterEncoding=utf8
           &connectionCollation=utf8mb4_unicode_ci
           &characterSetResults=utf8mb4
           &useSSL=false
           &allowPublicKeyRetrieval=true
           &serverTimezone=Asia/Seoul
>>>>>>> 083137365b1c481e8da87893406bbc26f1f27a30
    username: ${DB_USER:passit_user}
    password: ${DB_PASSWORD:passit_password}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: 8084

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.company.template: DEBUG
    org.springframework: INFO
