apiVersion: v1
kind: Service
metadata:
  name: service_chat-service
  labels:
    app: service_chat-service
spec:
  type: ClusterIP
  selector:
    app: service_chat-service
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
