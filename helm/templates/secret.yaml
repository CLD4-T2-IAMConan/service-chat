apiVersion: v1
kind: Secret
metadata:
  name: {{ include "service-chat.fullname" . }}-secret
  labels:
    {{- include "service-chat.labels" . | nindent 4 }}
type: Opaque
stringData:
  db-user: {{ .Values.database.user | quote }}
  db-password: {{ .Values.database.password | quote }}
  {{- if .Values.redis }}
  redis-password: {{ .Values.redis.password | quote }}
  {{- end }}
